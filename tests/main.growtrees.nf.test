nextflow_process {

    name "Test Process growtrees"
    script "main.nf"
    process "growtrees"

    test("Should generate trees") {

        when {
            params {
            }
            process {
                """
                input[0] = [ 'B6-16', 
                file("${projectDir}/tests/data/snp_only.fas"), 
                file("${projectDir}/accessory/infer_MP_nucleotide_200x.mao"), 
                file("${projectDir}/accessory/analyze_user_tree_MP__nucleotide.mao")]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
