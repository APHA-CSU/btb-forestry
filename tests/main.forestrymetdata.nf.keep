nextflow_process {

    name "Test Process forestryMetdata"
    script "main.nf"
    process "forestryMetdata"

    test("Should generate metadata") {

        when {
            params {
                today = "01Jan25"
            }
            process {
                """
                input[0] = [ true ]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
