nextflow_process {

    name "Test Process splitclades"
    script "main.nf"
    process "SPLIT_CLADES"

    test("Should split clades") {

        when {
            params {
            }
            process {
                """
                input[0] = file("${projectDir}/tests/data/Allclean_exp.csv")
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
