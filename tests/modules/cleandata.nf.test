nextflow_process {

    name "Test Process cleandata"
    script "main.nf"
    process "CLEAN_DATA"

    test("Should clean up input files") {

        when {
            params {
                today = "01Jan25"
            }
            process {
                """
                input[0] = go
                input[1] = file("${projectDir}/tests/data/subtest.csv")
                input[2] = params.today
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
